#!/usr/bin/env bash
# This script displays the part of the filenames after the first dash in the current directory
for file in *-*; do
  if [ -f "$file" ]; then
    # Extract and display the part of the filename after the first dash
    name_after_dash="${file#*-}"
    echo "$name_after_dash"
  fi
done
