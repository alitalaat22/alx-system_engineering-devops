#!/usr/bin/env bash
# قائمة جميع الملفات التي تحتي على شرطة في اسم الملف
for file in *-*; do
    # التحقق من أن الملف ليس مخفيًا
    if [[ -f "$file" && "$file" != .* ]]; then
        # استخراج الجزء بعد الشرطة الأولى
        name_after_dash="${file#*-}"
        echo "$name_after_dash"
    fi
done
